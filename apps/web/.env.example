# Web app environment variables (copy to .env.local and fill in).
# NEVER commit `.env.local` or any file containing real secrets.

# Public — exposed to the browser. Use a stable origin URL.
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# MongoDB — server-only. Used by Mongoose in later phases.
MONGODB_URI="mongodb+srv://USER:PASSWORD@CLUSTER.mongodb.net/face_attendance?retryWrites=true&w=majority"

# Better Auth — generate with: openssl rand -base64 32
# BETTER_AUTH_URL must match NEXT_PUBLIC_APP_URL in dev.
BETTER_AUTH_URL="http://localhost:3000"
BETTER_AUTH_SECRET="replace-with-a-long-random-string"

# Google OAuth (https://console.cloud.google.com/)
GOOGLE_CLIENT_ID="google-oauth-client-id.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="google-oauth-client-secret"

# Internal Face Service
FACE_SERVICE_URL="http://localhost:8001"
FACE_SERVICE_SECRET="replace-with-a-long-random-shared-secret"

# Biometric encryption key (required from Phase 4.2+)
# Generate locally with: openssl rand -base64 32
# This key is independent from other secrets (BETTER_AUTH_SECRET, FACE_SERVICE_SECRET, etc.)
# The key must be exactly 32 random bytes, base64-encoded.
# DO NOT use a placeholder value in production.
BIOMETRIC_ENCRYPTION_KEY="replace-with-a-32-byte-base64-key"
